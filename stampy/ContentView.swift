import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            Text("PostStamp")
                .navigationTitle("PostStamp")
        }
    }
}

#Preview {
    ContentView()
}
